24-05-30
git init
git remote add origin "원격지 Repository 주소"
** node_modules / 등록 필요없음
.gitignore 생성, 
git add *
git commit -m "작업 내용"
git push origin master(or main)
** github 계정 등록
git config --global user.email "khn010919@gmail.com"
git config --global user.name "kimhaneu1"

** 계정 설정 확인
git config user.name
